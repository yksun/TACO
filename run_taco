#!/usr/bin/env bash
# TACO - Telomere-Aware Contig Optimization
# Thin wrapper that delegates to the Python entry point.
# Usage: ./run_taco [options]   OR   python3 -m taco [options]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 -m taco "$@"
